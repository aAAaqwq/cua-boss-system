__pycache__/
*.pyc
.DS_Store
*.log
.env
# 本地话术配置（从 reply-templates.json 复制后自定义，gitignore）
reply.json
# 本地筛选配置（从 filter-template.json 复制后自定义）
filter.json
# 本地评分配置（从 scoring-template.json 复制后自定义）
scoring.json
# 本地岗位配置（从 jobs-template.json 复制后自定义）
jobs.json
# 数据库备份（含真实候选人数据，不提交）
data/backups/
# 候选人简历（含真实 PII，不提交）
data/resumes/
# 候选人数据库（含真实 PII：姓名/电话/微信/简历正文，不提交；init_db() 启动时自建）
data/candidates.db
# 云同步待补推队列（含上云 payload = 真实 PII，不提交）
data/cloud_queue.jsonl
web/config.js
data/.cloud_auth.json

# 桌面端本地构建物(用 desktop/build_app.sh 生成)
desktop/伯乐.app/
